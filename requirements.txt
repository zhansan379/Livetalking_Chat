python-dotenv
pyyaml
#torch-ema
##ninja
#trimesh
#opencv-python
#tensorboardX
numpy 
#pandas
tqdm
#matplotlib
#PyMCubes
#rich
#dearpygui
#packaging
scipy
#scikit-learn

#face_alignment
#python_speech_features
##numba
resampy
pyaudio        # play_music 工具：本机声卡输出
soundfile==0.12.1
einops
#configargparse

#lpips==0.1.3
#imageio-ffmpeg

transformers #==4.46.2
edge_tts
opencv-python-headless
aiortc
aiohttp_cors

#ffmpeg-python
#omegaconf
diffusers
accelerate

librosa
openai
#websocket-client
websockets>=14   # doubao TTS 用 deprecated 别名仍可用；≥13.4 才能与 chromadb 并存
dashscope
# 模拟面试题库向量库（chromadb；本地 onnx 内置模型向量化，首建索引需联网下载一次模型）
chromadb
# web_search 依赖；lxml 需 <6（6.x 的 DLL 在本机 uv Python 上加载失败）
ddgs
lxml<6
# MCP 外部工具接入（agent/mcp.py）：连接 stdio/sse/http MCP 服务器复用其工具
mcp
#gradio_client
#azure-cognitiveservices-speech

#typeguard==2.13.3
#onnxruntime-gpu

# Local ASR server (SenseVoice/FunASR) — Issue #604
# Optional: remove these lines if you don't need local ASR
funasr
modelscope
# 关键词唤醒（KWS）服务 — server/kws.py 使用 sherpa-onnx 开放词表 KWS
sherpa-onnx
pypinyin
